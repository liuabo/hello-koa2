const Tag = require('../model/queryPage')
const Op = require('sequelize').Op

module.exports = async (ctx, next) => {
    const user = ctx.request.body
    // ctx.body = {
    //     code: res ? 200 : 401,
    //     desc: res ? '登陆成功' : '账号或密码错误'
    // }
}
